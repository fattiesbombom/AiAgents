# Copy to .env and fill with your Supabase project values (never commit .env).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# FastAPI trigger + dashboard API (default http://127.0.0.1:8000 if unset).
# VITE_API_BASE_URL=
